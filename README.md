# El Cielo Menu

Mobile-first static menu rebuilt from the supplied screenshots.

Files:
- `index.html` layout
- `style.css` visual design
- `menu.js` all menu content
- `app.js` navigation and interactions

Future price/menu edits can be made mainly in `menu.js`.
